# Daily Note

Worked on #machine-learning and #nlp/transformers today, plus the
#project/astrolabe indexing pipeline.

```js
const fake = "#not-a-real-tag inside a fenced code block"
```

An inline snippet `#alsofake` must be ignored, and the bare number #2024
is not a tag either.
